<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.Eparking.dao.TransactionMapper">
    <insert id="insertTransactionPLO" parameterType="com.project.Eparking.domain.request.RequestPLOTransaction" >
        INSERT INTO PloTransaction (method, ploID, status, depositAmount, transactionDate, transactionResultDate)
        VALUES (#{method}, #{ploID}, #{status}, #{depositAmount}, #{transactionDate}, #{transactionResultDate});
    </insert>
    <select id="getTransactionPLOByID" resultType="com.project.Eparking.domain.PLOTransaction">
        SELECT historyID,method,ploID,status,depositAmount,transactionDate,transactionResultDate
        FROM PloTransaction
        WHERE ploID = #{ploID} AND status = #{status}
    </select>
</mapper>